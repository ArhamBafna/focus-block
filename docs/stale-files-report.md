# Stale / Idle Files Report

Generated: 2026-08-25. Read-only scan. Nothing deleted. Confirm before removing.

Working tree is clean (`git status` empty). No orphaned branches (`main` only).

## Safe to remove (regenerable)

| Path | Size | Why safe |
|---|---|---|
| `target/` | 7,994.8 MB | Rust/Cargo build cache. Fully regenerated on next `cargo build`. Biggest win. |
| `apps/desktop/node_modules/` | 173.0 MB | Dependency cache. Regenerated with `npm install`. |
| `apps/extension/node_modules/` | 155.5 MB | Dependency cache. Regenerated with `npm install`. |
| `apps/desktop/dist/` | 0.3 MB | Vite build output. Regenerated by desktop build script. |
| `apps/extension/dist/` | 0.5 MB | Extension bundle output. Regenerated by `npm run build` in `apps/extension`. |
| `apps/desktop/src-tauri/gen/` | 0.3 MB | Tauri-generated scaffolding/schemas. Regenerated on next tauri build. |

Total reclaimable: ~8.3 GB.

## Human decision needed

| Path | Size | Notes |
|---|---|---|
| `graphify-out/` | 4.0 MB | Generated code-graph index. Removable, but re-index takes time; actively used by agent workflow (`graphify query`). Recommend keep. |
| `.scratch/questions-issues-3-15.md` | 3 KB | Scratch note from old session. Untracked? Check content before deleting. |
| `design-extract-output/screenshots/full-page.png` | 5.1 MB | **Tracked in git** (largest committed file). Do not delete: referenced by design workflow per root `AGENTS.md`. Optional improvement: move to Git LFS or drop from history in a separate cleanup PR. |

## Not stale (do not remove)

- `service/focus-service/` — native messaging host source (Rust), referenced by install scripts.
- `crates/`, `docs/`, `.github/`, `.agents/` — active sources/config.
- `design-extract-output/` — design reference required by project instructions.
- No `*.log`, `*.tmp`, `*.bak` files found outside caches.

## Suggested cleanup command (run only after confirmation)

```powershell
Remove-Item -Recurse -Force target, apps\desktop\node_modules, apps\extension\node_modules, apps\desktop\dist, apps\extension\dist, apps\desktop\src-tauri\gen
```
